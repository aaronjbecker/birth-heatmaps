# Deployment configuration
# Copy this file to .env and configure before running deploy.sh

# SSH connection string for the deployment server
DEPLOY_SERVER=USER@IP

# Local port for SSH tunnel to registry (default: 5001)
# Port 5001 is used by default to avoid macOS AirPlay Receiver conflict on port 5000
# LOCAL_REGISTRY_PORT=5001

# Registry host (auto-detected if not set):
#   - macOS/Windows (Docker Desktop): host.docker.internal (default)
#   - Linux (native Docker): localhost (default)
# Override if needed:
# REGISTRY_HOST=host.docker.internal

# IMPORTANT: Add to Docker insecure registries:
#   macOS/Windows: "insecure-registries": ["host.docker.internal:5001"]
#   Linux: "insecure-registries": ["localhost:5001"]