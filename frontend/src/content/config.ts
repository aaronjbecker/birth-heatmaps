/**
 * Astro Content Collections configuration.
 * 
 * This project doesn't currently use content collections.
 * Chart images are stored in src/assets/charts/ as static assets.
 * See src/lib/chart-config.ts for chart configuration.
 */

export const collections = {};
